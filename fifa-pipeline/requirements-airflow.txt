# ============================================================
# Python packages installed INSIDE the Airflow Docker container
# These are the deps our DAG tasks need at runtime.
# ============================================================

# Core data
pandas==2.2.2
numpy==1.26.4
pyarrow==15.0.2

# PySpark (tasks 2 — spark_process)
pyspark==3.5.1

# Database
sqlalchemy==2.0.30
psycopg2-binary==2.9.9

# dbt (task 3/4 — dbt_run, dbt_test)
dbt-core==1.8.3
dbt-postgres==1.8.3

# ML (tasks 5/6 — retrain_model, update_predictions)
scikit-learn==1.4.2
xgboost==2.0.3
joblib==1.4.2

# Utilities
python-dotenv==1.0.1
requests==2.32.3
